<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permiso para conexion a api--> 
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GIO"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.EmpleadoHomeActivity" />
        <activity android:name=".activities.EncargadoHomeActivity" />
        <activity android:name=".activities.ObrasActivity" />
        <activity android:name=".activities.EquipoActivity" />
        <activity android:name=".activities.PerfilActivity" />
        <activity android:name=".activities.TareasActivity" />
        <activity android:name=".activities.CalendarioActivity" />
        <activity android:name=".activities.IncidenciasActivity" />
        <activity android:name=".activities.TareasEquipoActivity" />
        <activity android:name=".activities.HorasMensualesActivity" />

    </application>

</manifest>